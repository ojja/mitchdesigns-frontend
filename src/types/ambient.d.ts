// Ambient module declarations for packages without bundled types.
// Note: `server-only` is the official Next.js package — this stub satisfies
// the type checker before `pnpm install` is re-run with the updated lockfile.
declare module "server-only";
